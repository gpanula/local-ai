"""Shared core library for the Local AI MCP tooling.

Single source of truth for workspace-path validation, JSON-RPC transport to the
Ollama MCP server, terminal-mcp streaming, and script sanitization. Consumed by
both the ``mcp_cli`` command package and ``mcp_ollama/server.py``.
"""
